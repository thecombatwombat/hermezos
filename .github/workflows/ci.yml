name: CI

# This workflow is disabled by default. To enable it:
# 1. Uncomment the 'on' trigger section below
# 2. Or trigger manually via GitHub Actions UI

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main ]

# Manual trigger
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Lint with ruff
      run: |
        ruff check src/ tests/
        ruff format --check src/ tests/

    - name: Type check with mypy
      run: |
        mypy src/

    - name: Run tests
      run: |
        pytest

    - name: Validate HermezOS rules
      run: |
        python -m hermezos.cli validate

    - name: Pack rules (example)
      run: |
        python -m hermezos.cli pack . --json - > hermez-report.json

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results-${{ matrix.python-version }}
        path: |
          hermez-report.json
          htmlcov/
        retention-days: 30

  # Uncomment this job to enable MCP server testing
  # test-mcp:
  #   runs-on: ubuntu-latest
  #   needs: test
  #
  #   steps:
  #   - uses: actions/checkout@v4
  #
  #   - name: Set up Python 3.12
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: "3.12"
  #
  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -e ".[dev,mcp]"
  #
  #   - name: Test MCP server
  #     run: |
  #       timeout 10s python -m hermezos.mcp.server &
  #       sleep 2
  #       # Add MCP client tests here when available